/*

function greet(name) {
  console.log(`Hello ${name}`);
}

function greetKevin (greetFn) {
  const name = 'Kevin';
  greetFn(name);
}


greetKevin(greet); //this is a synchronous callback as a callback that is executed immediately is called a synchronous callback as the control goes to the higher order function the callback function is executed immediately


// a practical example of synchronous callback function
// let numbers = [6,2,3,5,4,1];
// numbers.sort((a,b) => a - b);
// console.log(numbers);


*/


/*
//asynchronous callbacks are used when we need to delay the execution of callback function

//example
function callback() {
  document.getElementById("demo").innerHTML  ="Hello World";
}
document.getElementById("btn").addEventListener('click', callback); //dont immediately run the callback function only when clicked it runs


*/
